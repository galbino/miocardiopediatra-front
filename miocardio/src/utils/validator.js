import React from "react";

export default function blockWhiteSpace(e) {
    if(e.which === 32) {
      e.preventDefault();
    }
}

export default validator = (props) => {
    const { nome, senha } = props;



}

